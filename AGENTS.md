# Codex Project Instructions — 数学建模国赛

本仓库用于全国大学生数学建模竞赛（CUMCM）建模、实验、论文与提交材料协作。

## 工作原则

1. **事实优先于文字。** 题面、正式附件、代码输出、冻结结果和经过核验的文献是事实源；不得为了让论文“更好看”而修改或补造结果。
2. **代码与论文必须一致。** 论文中的模型、目标函数、约束、时间尺度、参数、结果和图表必须可从当前代码与正式实验追溯。
3. **优先复用现有工作流。** 修改前先检查 `README.md`、`docs/`、`state/`、`experiments/` 和相关脚本，避免另起一套重复流程。
4. **保持可复现。** 正式结果尽量通过仓库脚本生成并记录；不要手工修改正式输出去迎合论文叙述。
5. **不静默修模。** 在论文润色任务中发现数学、数据或实现问题时，明确标记问题，不要把模型错误包装成通顺文字。
6. **建模、写作、评审分离。** 思路完善、论文改写、盲审评分分别使用不同 Skill，避免角色混淆。

## Skill 路由

### 建模思路完善 / 模型选型 / formal 与参考稿整合

当用户要求“完善建模思路”“把思路变成模型”“模型选型”“合作版方案”“补全目标函数/约束/实验设计”“比较 formal 与参考方案”，或当前只有自然语言方案尚未形成可实现数学模型时，使用：

`.agents/skills/cumcm-modeling-coauthor/SKILL.md`

使用该 Skill 时：

- 队内 `formal`、代码和实验为主方案；网上范文/论文只作参考；
- 显式区分 `[题面事实]`、`[队内方案]`、`[外部参考]`、`[推断/待验证]`；
- 先建立每问的输入、输出、决策、状态、目标、约束、信息集、依赖和验证，再决定模型；
- 模型必须写到代码可直接实现：变量、参数、目标函数、约束、边界、求解方法、baseline 和实验计划完整；
- 预测/滚动优化任务必须审计未来信息泄漏和真实状态更新；
- 外部方法只有在修复缺陷、补足题目要求或带来可测量收益时才吸收；争议项标记 `[待队伍决策]`。

### 论文写作 / 改写 / 降模板化表达

当用户要求撰写、润色、压缩、优化摘要、结果分析、模型评价、统一文风或降低模板化/AI化表达时，使用：

`.agents/skills/cumcm-academic-humanizer/SKILL.md`

使用该 Skill 时：

- 保持数字、公式、图表编号、引用、单位和已冻结结果不变；
- 缺少证据时标记 `[待核验]`，不要编造；
- 对定量结论执行 claim↔evidence 检查；
- 当前 C 题额外检查功率/电量、小时→10分钟时间尺度、预测信息泄漏、波动电价信息边界、SOC 跨日连续和四问口径一致性。

### 数模评委 / 盲审 / 打分 / 奖项竞争力判断

当用户说“数模评委”“评委视角”“盲审”“审稿”“打分”“判断奖项”“挑刺”，或显式提到 `$数模评委` 时，使用：

`.agents/skills/cumcm-judge/SKILL.md`

评委 Skill 默认：

- **只评审，不直接替作者改写正文**；
- 复用 `templates/reviewer_scorecard.csv` 的 8 个维度进行 100 分制内部评分；
- 优先检查 P0/P1 正确性与证据链问题，再讨论文风；
- 奖项判断只能作为内部竞争力校准，不能承诺真实奖项；
- 如果代码、数据、参数或冻结结果发生变化，旧评分自动视为过期，需要复审。

## 推荐协作顺序

当前只有思路时：

1. 队内思路 / `formal` / 外部参考 → `cumcm-modeling-coauthor`；
2. 建立最小 PoC、baseline 和正式实验；
3. `cumcm-judge` 对模型正确性、信息边界和证据链做预审；
4. 修复 P0/P1 模型、数据、代码问题并重新运行受影响实验；
5. `cumcm-academic-humanizer` 把已验证模型与结果写入论文；
6. `cumcm-judge` 对终稿复审；
7. 最后通过 G1–G6 和提交前检查。

已有完整初稿时，可从 Judge 盲审开始。不要让 Humanizer 为缺失证据补“合理解释”，也不要让 Judge 因文风漂亮而忽略正确性问题。

## 代码与实验任务

纯代码实现、数据处理、模型调参、求解器配置、实验运行时，不必调用论文 Humanizer。优先确保：

- 输入数据来源明确；
- 时间索引与单位明确；
- 随机种子和依赖可复现；
- 输出写入约定目录；
- 正式实验与草稿实验可区分；
- baseline 与主方案在同一数据版本、评测区间和指标口径下比较。

若代码或数据变化可能改变论文主要结论，应在后续重新执行 Judge 评审，而不是沿用旧评分。

## 结果与提交

提交或写入正式论文前，优先检查仓库现有流程：

- `python scripts/result_freeze.py check`
- `python scripts/gatekeeper.py status`
- `powershell -ExecutionPolicy Bypass -File scripts/final_check.ps1 -ForbiddenTerms "学校全名","队员姓名"`

仓库当前质量门控中，G5 要求论文证据链闭合，G6 要求终稿审计，并已有 `templates/reviewer_scorecard.csv` 作为评委评分证据。评委 Skill 应优先复用这些已有材料。

若这些脚本或仓库结构发生变化，以当前仓库最新版为准。
